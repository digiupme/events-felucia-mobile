name: event_checkin
description: "Event Check-in"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.2.2
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  http: ^1.6.0
  email_validator: ^3.0.0
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true


  assets:
    - images/
